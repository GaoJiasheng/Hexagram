# hexagram — 观象 · 个人学习站

个人学习站集合,**配置驱动的分站平台**(v14)+ **分组隔离**(v15):十组——易道(易经研习 yijing 默认 + 道藏研读 dao,**门户互切**,因「桥」需同组)/ 儒 ru / 佛 fo / 心 xin / 法 fa / 墨 mo / 兵 bing / 纵横 zong / 医 zhongyi(中医,观和/杏林赭,守「研习不诊疗」铁律)/ 谋 moulue(谋略杂纂,观谋/黯灰紫,《天下无谋》托名伪书 5 部,显著框注「托名·疑现代伪作」、文献批判视角不作权术教程)。**左上角 logo 全站链向总门户 `/hexagram`**(v1.24,owner 全局导航;原「隐藏门户」改为各页可达,跨组隔离遂不再严格——如需恢复硬隔离另加开关)。除易道外每组皆**独立单站、平级**(法墨兵纵横原 v18 拟同「诸子」组互切,v20.1 owner 拍板拆为各自独立组,门户列为平级卡片、不互切)。站点在 `src/sites/registry.js` 注册(含 `group` 字段),加站零改平台代码。**门户只列当前站所属组,跨组零可见链接**(诸组两两不互链);`activeGroup` 域名优先(HOST_GROUPS,用户填真实域名)路径兜底,一份构建多域名指向。组内不互链,唯一切换点是门户(v4 §3);**唯一例外是「桥」(v8,易道组内)**:参同契/阴符经注疏气泡内的卦名可单向跳转易经卦页。纯前端(React 19 + Vite),无后端,用户数据只存 localStorage,可静态托管。**佛/儒/心/诸子经文内容已齐**(儒典四书+孝经+荀子+颜氏家训+近思录、释典十经〔心经/金刚经/坛经+遗教三经〔四十二章经/遗教经/八大人觉经〕+阿弥陀经+禅宗双偈〔信心铭/永嘉证道歌〕+维摩诘经〕、阳明传习录+大学问、诸子十三书〔法墨兵纵横,含六韬+慎子+尹文子+战国策选 18 篇〕、中医六书〔内经选篇/伤寒论/本草经/金匮要略/难经,守研习不诊疗铁律〕全 status done、译注延齐备(v1.23.0,后 v1.33.0 补六韬+金匮);另**谋略杂纂组**收《天下无谋》托名伪书 5 部〔罗织经/小人经/权谋术/韬晦术/止学〕,显著框注「疑现代伪作」、取文献批判视角,v1.24.0)。

## 如何加一个新站(v14 §1·§4)

平台已抽成「壳(manifest)+ 通用阅读器 + registry 管线」,加一家读经类新站(如儒/佛)≈ 纯内容,**不碰平台代码**:
1. `src/sites/registry.js` 加一条 `{key, group, brand, portalTitle, portalDesc, home, prefix, accent, switchLabel, hasSearch, nav, mobileNav, mobileSwitch}`。**group 决定隔离**:与谁同门户填同 group,要独立则单独 group;独立域名访问填 `HOST_GROUPS['域名']='该group'`。
2. `src/index.css` 加 `[data-site="新key"] { --cinnabar: var(--某色) … }` 换肤(主色)。
3. 数据:原文走管线(仿 fetch-dao,抓取源+切片配置写脚本)、白话译文/字词注疏/每章延伸为人工内容;书目元数据仿 `src/data/dao/texts.json`(slug/title/sections/…),check-data 的书单从它派生,加书免改校验。
4. 阅读页:薄包装调 `src/features/reader/ClassicReader.jsx`(paged/single 两模式),传各自 loader/anchors/yanyi/header;首页/书架仿 DaoHomePage。
5. 在 App.jsx 加该站的 `<Route>`(每站页面是各自代码,路由需显式列)。
6. 跑 `check-data`——覆盖仪表自动纳入新书。

## 唯一规格来源

**docs/yijing-design.md(一期 M1–M3)、docs/yijing-design-v2.md(二期 M4–M6)、docs/yijing-design-v3.md(三期 P0–P5)、docs/design-v4.md(四期:注释层/源流/多模块门户与道藏框架)、docs/design-v5.md(五期:经传逐段注疏层)、docs/design-v6.md(六期:道藏内容期)、docs/design-v7.md(七期:道藏译注收官)、docs/design-v8.md(八期:道藏→易经桥)、docs/design-v9.md(九期:筮例与故事)、docs/design-v10.md(十期:工具与交互期)、docs/design-v11.md(十一期:工程打磨期)、docs/design-v12.md(十二期:推演上手引导)、docs/design-v13.md(十三期:道藏单页阅读与每章延伸)、docs/design-v14.md(十四期:分站平台化)、docs/design-v15.md(十五期:三教分站与分组隔离)、docs/design-v16.md(十六期:儒典四书译注与延伸,已实现 v1.17.0)、docs/design-v17.md(十七期:释典三经译注与延伸,v1.18.0)、docs/design-v18.md(十八期:诸子百家——法/墨/兵/纵横,v1.20.x)、docs/design-v19.md(十九期:中医典籍,v1.23.0)、docs/design-v20.md(二十期:谋略杂纂·托名伪书,v1.24.0)与 docs/design-v21.md(二十一期:《赛博·百家争鸣》跨派对辩,v1.51.0 起)是页面、交互、视觉、推演规则、数据结构的唯一规格(以上设计稿对应内容均已落地)。** v2–v4 是增量稿,视觉/组件/数据约定沿用 v1。实现任何页面前先读对应章节,不要自行发明视觉风格或交互;引擎规则(八宫/纳甲/梅花/大衍/金钱卦)必须照设计稿的规则表实现,严禁凭记忆补规则。每个里程碑完成后逐条核对其验收清单。

## 当前状态(随进度更新此节)

- [x] M0 脚手架:路由占位壳、设计 tokens(src/index.css)、favicon、数据管线
- [x] M1 读:HexagramFigure 组件、六十四卦总览、卦详情页、导航/主题/译文开关
- [x] M2 推:推演引擎(纯函数 + 单测)、推演工作台
- [x] M3 通:经传阅读、八卦基础、我的、全局搜索、今日一卦(已发 v1.0.0)
- [x] M4 明:术语层(glossary.json + TermTip)、三才/爻位身份/贞悔、卦主 schema(v2 §3–§4)
- [x] M5 宫:八宫纳甲引擎(bagong/najia)+ 详情页纳甲节 + 总览八宫视图(v2 §5)
- [x] M6 占:梅花易数(meihua + lunarAdapter)+ 学堂改版(河洛/消息卦/大衍/名词表)(v2 §6–§7)
- [x] 三期 P0–P5 读练用体系:金钱卦(jinqian)、工作台六法(梅花引导/大衍/金钱三入口)、学堂七篇(新增梅花/金钱教学页)、QuizCard 练习、学算双向跳转、研习进度(progress)(v3 全文)
- [x] 四期:64 卦译文全部完成(含用九/用六)、经文字词注释层(zhushi.json + AnnotatedText)、易学源流页、多模块门户 + 道藏框架(六部经典占位)(v4 全文)
- [x] 易经内容收官:经传 204 段全译(scripts/authored/classics-translations.json,按 book→章号→段序合并)、乾坤文言传 35 段全译(translations.json 的 wenyan 数组)、序卦/杂卦按卦引文(fetch-data 自动解析,64/64)、卦主 64 卦标注(hexagram-meta.json 的 guazhu 字段 + LineRow 赭石小章)
- [x] 五期:经传逐段注疏层(v5 全文,已发 v1.4.0)——753 单元 18 批全部审毕,762 条锚注(彖 61/64·大象 55/64·小象 243/386·文言 21/35·系辞 57/60·说卦 37/50·序卦 6/63·杂卦 9/31,未注单元为审后无拦路词留空);数据在 src/data/yijing/zhushi-anchored/(人工维护),组件 AnnotatedText anchors 锚定模式 + ref 复用全局词条,check-data 锚点校验兜底。**修订注疏照 v5 §5 风格规范,改完必过 check-data**
- [x] 六期:道藏内容期(v6 全文,已发 v1.5.0)——六部原文录入(scripts/fetch-dao.mjs + scripts/lib/wikisource.mjs 共享库,与易经管线同缓存);章节阅读器 DaoReadPage(/dao/:slug/:chapter,复用经传阅读器模式);道德经 81 章全译(scripts/authored/dao-translations.json)+ 190 条锚定注疏(src/data/dao/zhushi-anchored/daodejing.json,**无 ref**,模块不互链)。其余五部仅原文(status: partial),译注照 v6 §4–§5 工序逐部展开
- [x] 八期:道藏→易经桥(docs/design-v8.md,已发 v1.7.0)——参同契 45 处+阴符经 1 处卦名桥点(注疏条目 hex/to 字段),气泡尾部单向跳转易经卦页;check-data 校验 hex 1–64、易经侧禁用桥字段。**新增桥点照 v8 §2 规则(仅道藏侧、段内首次出现、不与既有锚点重叠)**
- [x] 七期:道藏译注收官(docs/design-v7.md,已发 v1.6.0)——五部译注全齐:清静经 6 段/感应篇 14 段/阴符经 3 段(管线修复:校对页断行致句子腰斩,每章合并为一段)/庄子内篇 53 段/参同契 45 段,六部 status 全部 done;道藏译文 562 段全覆盖,锚注共 550 条。**修订工序照 v6 §4–§5 与 v7 §2 分书风格,改完必过 check-data**
- [x] 十六期:儒典四书(docs/design-v16.md,已发 v1.17.0)——内容期,标准对齐道藏。**通用 corpus 基建一次落地**:`scripts/fetch-corpus.mjs <key>`+`scripts/corpus/<key>.config.mjs`(每源页一章,剔标题/章号/页脚/`<ref>`校勘/多变体`-{zh-hans}-`,补异体字 愼/擧);前端 `src/features/reader/corpus.js`(glob 限 {fo,ru},道藏不牵入)+ `CorpusTextPage/ReadPage/SinglePage`+通用 `YanyiBlock`+ App.jsx `/fo|/ru` 路由;check-data 泛化 `checkReadingCorpus`+`checkCorpusAnchors`(道藏输出不变)。**四书全译注延**:论语 507 段/20 篇延伸/960 注疏、孟子 743 段/14 卷/954 注疏、大学 11 段、中庸 34 段;底本论语孟子取维基文库通行子页、大学中庸取《礼记》古本(章句本缺经/带注)。论语孟子由**并发 workflow**(每篇/卷 译+校双代理)产出后程序化装配(term 须原文精确子串、note≤40、段数对齐,违规即弃)。**改儒内容照 v16 分书风格(朱熹《四书章句集注》为主流),逐批 check-data 过才 commit**
- [x] 十七期:释典三经(docs/design-v17.md,已发 v1.18.0)——复用 v16 通用 corpus 路,守 v17 §0 铁律(研习不宣化、不下吉凶/果报断语)。管线加 `splitHeadings`(金刚经 32 分按 `====X分====` 切章、去『』夹注、跳「正文/外部链接」)+ `PIN_TITLE_RE`(剔品题行)。**三经全译注延**:心经 10 段/32 注疏、金刚经 253 段/32 分延伸/337 注疏、坛经 555 段/10 品延伸/519 注疏(并发 workflow 产出+程序化装配:term 须原文子串、note≤40、段数对齐;超长 note 句读处截断、重叠锚点去重)。**底本**(用流传最广本):心经**玄奘略本**(从大正藏 djvu 第864页 splitHeadings 取经文、跳明太祖序;wikisource.mjs 加 normalized 标题映射、fetch-corpus 加跳`序$`标题+剔章末重复经题);金刚经罗什/江味农校定本;坛经宗宝本 `六祖壇經/{10品}` 子页。**改佛内容守 §0 铁律,逐批 check-data 过才 commit**
- [x] 阳明心学组 + 扩书 + 诸子脚手架(已发 v1.19.0)——**阳明心学拆为独立第四组**(xin,观心/靛紫,/xin,从儒拆出):传习录 3 卷 967 段全译/3 卷延伸/1296 注疏(并发 workflow 切 5 片+程序化装配 `scripts/assemble-newtexts.mjs`:term 须原文子串、note≤40 句读截断、同段重叠去重、区间单元局部下标+start 还原)。**扩书**:儒+孝经(今文 18 章,22 段全译/18 延伸/78 注疏)、佛+四十二章经(高丽藏古本,50 段/1 延伸/34 注疏,守 §0 铁律)。**诸子百家组**(zhuzi,法 fa·墨 mo·兵 bing·纵横 zong,门户互切,v18):四站脚手架(观法/观兼/观兵/观衡 主色)+ 九书原文录入(韩非子 55/商君书 24/墨子 42/孙子 13/吴子 6/司马法 5/尉缭子 20/三略 3/鬼谷子 15;管线加 `LOSS_NOTE_RE` 剔亡篇占位、splitHeadings 跳「答話」吴问附录),status partial 可读,译注延后续批次。**规格 docs/design-v18(诸子)、design-v19(中医)为计划稿;诸子守思想史视角不作政治影射/权术教程,中医守「研习不诊疗」铁律**
- [x] 诸子百家译注延全成(已发 v1.20.0)——法/墨/兵/纵横**九书全译注延**(并发 workflow 189 单元/378 代理/19M token,一章一单元、>55 段大篇切片,译+校双代理,schema 强约束;`scripts/gen-zhuzi-wf.mjs` 按真实章段数生成脚本,复用 `scripts/assemble-newtexts.mjs` 装配):韩非子 773 段/55 延/2398 注、商君书 127/24/441、墨子 585/42/1270、孙子 74/13/223、吴子 43/6/122、司马法 68/5/152、尉缭子 86/20/227、三略 65/3/143、鬼谷子 25/15/82;共 ~5058 锚注(装配弃 14 term 未命中+1 重叠),八书 status done(战国策选 18 篇已于 v1.22.0 补齐,九书全 done)。守思想史铁律:如实呈现法家严刻/纵横机变,不作现代政治影射、不作权术教程。**改诸子内容守此铁律,逐批 check-data 过才 commit**
- [x] 诸子拆组 + 全站搜索 + 篇目标题 + 战国策收尾(已发 v1.20.1→v1.22.0)——① **诸子四家拆为独立平级组**(owner 拍板:法/墨/兵/纵横各自一组、不再门户互切,门户列 8 张平级卡;易道仍同组因「桥」);② **全站搜索接入 7 读经站**(`src/features/reader/corpusSearch.js`+`CorpusSearchPalette`,按 corpus 懒建索引、本组只搜本组,registry 7 站 hasSearch→true;道藏走自有数据层未接);③ **篇目显示章名**(CorpusTextPage/DaoTextPage 载 classics 取 title 渲染药丸格,`tocTitle` 纯序数〔道德经一章/二章〕回退序号;经传按名列卡无需改);④ **战国策选 18 篇收尾**(摘录管线 stripStarTemplates 剔士礼居本校注+pickHeadings 选章;90 段全译/18 延/324 注,纵横满)。**新读经站默认接搜索(hasSearch+CORPUS_SEARCH_SITES);新分章书篇目自动显示 title**
- [x] 中医典籍组(已发 v1.23.0,第九组)——独立组 zhongyi(观和/杏林赭),守 v19 §0「**研习不诊疗**」铁律(首页+各书小传「⚠ 非医疗建议」声明;本草经「主治…」、伤寒论方剂属原典照译,但注疏/延伸不述功效用法用量、不下病症/疗效断语、不教自疗)。**四书全译注延**:素问 15 篇(卷分组 `pickHeadings` 跨卷选核心篇,异体页名 欬論/痺論 按页字配)676 段、灵枢 8 篇 192 段、伤寒论 22 篇(单页 splitHeadings)984 段、神农本草经 18 类(6 部×3 品)359 药;并发 workflow 85 单元/170 代理/9.4M token,~3357 锚注、63 篇延伸全覆盖。`gen-zhuzi-wf` 加 `TIELU_YI` + 分书 styleRule(内经问答体/伤寒方剂只录名/本草主治照译诠释守铁律)+ 按 corpus 过滤生成。**改中医内容守研习不诊疗铁律,逐批 check-data 过才 commit。** 另:**docs/design-v20 谋略杂纂**(《天下无谋》维基有原文的 5 部:罗织经/小人经/权谋术/韬晦术/止学,owner 定收并显著框注「托名·疑现代伪作」、批判延伸不作权术教程)已于 v1.24.0 落地(见下条)
- [x] 谋略杂纂组 + 总门户入口(已发 v1.24.0,第十组)——**《天下无谋》托名伪书 5 部**(维基有原文者:罗织经 12 卷/小人经〔荣枯鉴〕10 卷/权谋术 13 篇/韬晦术/止学,~1.1 万字)译注延全成(并发 workflow 37 单元/74 代理/2.8M token,带 `TIELU_MOU` 伪书批判铁律:译文照译其权术面目、延伸批判性标真伪并指其偷换史实/泯善恶、绝不作权术教程)。**真伪框注**:texts.json `dubious:true` → ScriptureShelf 卡片 + CorpusTextPage 题解「⚠ 托名·疑现代伪作」徽标(`.dubious-badge`),首页副标说明文献批判视角。管线增强(他经不变):preResolve 清 `{{ProperNoun}}`/`{{Novel}}`/`<onlyinclude>`/`__TOC__` 等模板、跳目录行、新增 `chapterTitles`(罗织经 01..12→阅人卷一…)。**总门户入口**:左上角 brand-seal 全站链 `/hexagram`(原「隐藏门户」改各页可达,跨组隔离不再严格)。**改谋略内容守伪书批判铁律(不为伪书张目/不教施用),逐批 check-data 过才 commit**
- [x] 典籍拓展 Wave 1a:武经七书补全(六韬)+ 伤寒杂病双璧(金匮要略)(已发 v1.33.0)——规划见 `docs/expansion-ideas.md`(owner 圈定 §0/§1/§2,底本一律取流传最广通行本)。**两书 corpus 路全译注延**:六韬 60 篇(`兵`组,武经七书凑 6/7,李卫公问对卷下底本阙待补)译 60/延 60/注 259;金匮要略 25 篇(`医`组,与伤寒论凑「伤寒杂病论」)译 955/延 25/注 1287。并发 workflow 91 单元/182 代理/8.7M token,`scripts/gen-zhuzi-wf.mjs` 加 `['bing','liutao']`+`['zhongyi','jinkui']`(金匮挂伤寒论同款规则:方剂只录名/守研习不诊疗)→ `assemble-newtexts` 装配。管线两处增强(他书不变):`parsePageChapters` 加 `dropChapterRe`(六韬剔无正文卷题→60 篇)+ `mergeHeadingRe`(金匮 附方/附注 并入前篇→25 篇)。**金匮守研习不诊疗铁律**(方剂照原典录,注疏/延伸只作字词训诂、医史源流,不述功效用法用量、不下疗效断语;延伸均带「宜作医史文献训读、非为对照自诊」)。**拓展续:Wave 1b 道藏庄子外杂+列子、Wave 2 佛短篇、Wave 3 大学问、Wave 4+ 荀子/难经/各组扩充(见 expansion-ideas §2)**
- [x] 典籍拓展 Wave 1b:《庄子》补全(外篇+杂篇)+ 列子(已发 v1.34.0)——**道藏组扩至 9 部**(易道组内)。走 **fetch-dao 管线**(非 corpus):庄子外篇 15 篇 409 段、杂篇 11 篇 420 段(凑全《庄子》33 篇,子页 `莊子/篇名`,perPageChapter)、列子 8 篇 139 段(凑道家「老·庄·列」三经,子页 `列子/X篇`,张湛注本)。**全译注延**:外篇 译 409/延 15/注 744、杂篇 译 420/延 11/注 708、列子 译 139/延 8/注 435。**dao 译注与 corpus 同构**(`src/data/dao/classics` 形态一致),故 `gen-zhuzi-wf.mjs` 加 `dao` 分支(REF 郭象/成玄英/张湛、`TIELU_DAO`:不宗教宣化、不下吉凶/福报/成仙断语、不演内丹工法、列子今本如实标真伪存疑)+ `assemble-newtexts.mjs` 直接复用(corpus='dao' → 写 `dao-translations.json`/`dao/zhushi-anchored`/`dao/yanyi.json`);装配后 **re-run fetch-dao 合译文进 classics**、`daoAnchored.js` 加 3 import。并发 workflow 38 单元/76 代理/6.2M token。**改/扩道藏内容守 v9 §4(讲思想/寓言/源流不作信仰宣化吉凶断语)、注疏无 ref(模块不互链)、列子真伪如实存疑,逐批 check-data 过才 commit**
- [x] 典籍拓展 Wave 2+3:佛短篇一锅 + 阳明大学问(已发 v1.35.0)——**佛组扩至 9 经、心组扩至 2 书**。佛(corpus fo):遗教三经齐〔补佛遗教经22段/八大人觉经10段〕+ 阿弥陀经20段(罗什译,净土)+ 禅宗双偈〔信心铭25段/永嘉证道歌64段〕,全译注延(各 status done);心(corpus xin):大学问13段(阳明晚年纲领,问答体)。**管线增强**(他书不变):`fetch-corpus` 加 `dropParaRe`(逐段剔经题/撰人题,如证道歌首标题与「唐慎水沙门玄觉撰」)+ `stopParaRe`(遇匹配段截断其后,如阿弥陀经取罗什正文终「作礼而去」、剔所附往生咒与译咒题记);`gen-zhuzi-wf` 加 `fo` 分支(`TIELU_FO` 研习不宣化、不下吉凶/果报/往生承诺断语、不劝皈信)与 `xin` 分支(心学思想史视角、不作成功学/鸡汤发挥)。并发 workflow:fo 6 单元/12 代理/0.65M token、xin 1 单元/2 代理/0.13M token。**底本**:阿弥陀取罗什正文(往生咒附录非原译,不录);信心铭如实标拟托存疑;大学问含钱德洪跋(如实译并注为录者后记)。**改/扩佛内容守研习不宣化铁律(不下果报/往生劝信)、心学不作鸡汤,逐批 check-data 过才 commit**
- [x] 典籍拓展 Wave 4+5:荀子(儒最大缺口)+ 难经(中医)(已发 v1.36.0)——**儒扩至 6 书、中医扩至 6 书**。儒(corpus ru):荀子 32 篇 481 段(子页 `荀子/X篇`,性恶/隆礼重法/天人之分,gen-zhuzi-wf `ru` 分支:思想史视角、如实呈现不作现代借用、《成相》弹词《赋》隐语存体例);中医(corpus zhongyi):难经 81 难 257 段(单页 splitHeadings,问答体,`nanjing` styleRule 守研习不诊疗、脉学独取寸口作文献训诂)。译注延全成:荀子 译 481/延 32/注 1288,难经 译 257/延 81/注 638(零铁律违规)。并发 workflow:荀子 34 单元、难经 81 单元;**两大 workflow 并发触发 API 限流(校验/译文阶段部分单元失败),荀子 9 单元被 drop → `resumeFromRunId` 缓存 25、补跑 9 单元齐全;难经校验 29 难回退译文(数据完整、铁律守住)。教训:勿两大 workflow 齐发,Wave 6 起串行/小批。** 管线增强:`fetch-corpus` cleanLine 加**校勘脚注剔除**(难经第81难尾混入 19 条书级校记 `[1]字：原作…据《…》改`,整行剔 + 剥离正文内联校注锚 `[数字]`;经典正文不用 `[数字]` 故他书 no-op)。**新分章书的校勘记/脚注若以 `[数字]` 格式混入,cleanLine 已自动剔除;改/扩儒守思想史、中医守研习不诊疗,逐批 check-data 过才 commit**
- [x] 典籍拓展 Wave 6a:法家慎子/尹文子 + 道藏文子(已发 v1.37.0)——**法扩至 4 书、道藏扩至 10 部**。法(corpus fa):慎子 8 篇 75 段(势治残篇,威德/因循…+逸文,跋以 `dropChapterRe` 剔、知忠/君臣治要辑补)译 75/延 8/注 120、尹文子 2 篇 62 段(名法,大道上/下)译 62/延 2/注 96;道藏(fetch-dao):文子 12 卷 202 段(黄老·通玄真经,子页 `文子/卷N`)译 202/延 12/注 452。**三书凑一 workflow(23 单元/46 代理/4M token,单 workflow 无限流——印证「勿两大齐发」)**。dao 文子走 fetch-dao + `daoAnchored.js` 加 import;gen-zhuzi-wf 加 fa 慎子/尹文子(走通用法家思想史分支)+ dao `wenzi` case(黄老,真伪「旧疑伪托·1973 定州汉简证有古本而今本多异」如实标)。守铁律:法不作权术教程/政治影射、道藏不宣化、真伪如实(慎子辑补、尹文子伪托疑、文子定州汉简)。**Wave 6 续:颜氏家训/近思录(儒)、百喻经(佛)串行小批;待定本:维摩诘/黄庭/象山语录**
- [x] 典籍拓展 Wave 6b:儒家颜氏家训 + 近思录(已发 v1.38.0)——**儒扩至 8 书**。颜氏家训 20 篇 256 段(家训之祖,splitHeadings 跨 7 卷切 20 篇)译 256/延 20/注 755、近思录 14 卷 641 段(程朱理学阶梯,朱熹吕祖谦辑,chapterTitles 映卷名道体/为学…观圣贤)译 641/延 14/注 1101。单 workflow(39 单元/78 代理/5.7M token)。gen-zhuzi-wf ru 分支泛化(荀子/颜氏/近思录三 case)。管线:近思录卷首泄漏的卷名单字段(道体/为学…)`dropParaRe` 剔、注本卷首提要(「此卷论…」)保留照译。守儒家思想史(颜氏注时代局限不作现代育儿/成功学、近思录提要标注本所加)。**Wave 6 仍剩:百喻经(佛,可径做)+ 待 owner 定底本:维摩诘(罗什《维摩诘所说经》14 品)/黄庭经(内景or外景)/象山语录(子页缺失)**
- [x] 典籍拓展 Wave 6c:佛维摩诘经 + 道黄庭内景经(已发 v1.39.0)——**佛扩至 10 经、道藏扩至 11 部**。维摩诘所说经 14 品 245 段(罗什译,大乘居士不二法门;子页 `維摩詰所說經/01…14` 数字页,chapterTitles 映品名,dropParaRe 剔卷题/译者/品题)译 245/延 14/注 359;黄庭内景经 36 章(上清派存思养生七言韵语;**「全覽」是 `{{:子页}}` 转写壳**,改抓 36 个分章子页 `太上黃庭內景玉經/X章第N`)译 36/延 36/注 156。单 workflow(51 单元/102 代理/4.8M token)。gen-zhuzi-wf 加 fo `weimojie`(守研习不宣化)+ dao `huangting`(丹道隐语直译、不演工法、不下成仙断语)case;黄庭走 fetch-dao + daoAnchored.js 加 import。**底本**:维摩诘罗什本;黄庭取内景经(存思养生主经)。**经验**:维基「全覽/合页」常为转写壳(`{{:子页}}`),正文须抓分章子页。**Wave 6 仍剩:诗经(6d,三家诗辑校本需子段+poem 提取)、悟真篇(需 fetch-dao 合并数字诗题);百喻经维基子页空、象山子页缺,无源暂弃**
- [x] 产品/UX/国学三视角审查 + P0 修债(已发 v1.40.0,零内容变化)——内容扩至 ~55 部后三视角并发审查汇总 `docs/product-ux-review.md`(交叉命中最可信;不重复前两轮已实施项)。**P0 五项小修全做**:① 文子/黄庭 `texts.json` 漏填 `status:done`(致书架空徽标+漏出今日一章)补回;② `MasterPortalPage` 卡片描述改**从 texts.json 派生**(`siteDesc`:前 4 本+「等 X 部」,易经保留 tagline)——根治「加书忘改 portalDesc」(释典 4→10、儒 5→8、兵含六韬→6 等计数自动正确);③ **真伪标记全站统一**:texts.json 加中性 `caveat` 字段(列子/文子/阴符经/黄庭「成书存疑/辑佚」),`ScriptureShelf`/`DaoHomePage`/`CorpusTextPage`/`DaoTextPage` 渲染 `.caveat-badge`/`.dao-book__caveat`(弱灰,区别谋略 `dubious` 强批判红);④ 道藏 `DaoTextPage` 补「继续读」+续读章 `--current` 高亮(对齐 9 个 corpus 站,`getReadingProgress` 通用);⑤ 长 TOC 当前章 `useLayoutEffect` 自动滚入视野(`tocRef` 挂 `<nav>`+querySelector+算 scrollTop,**勿用 rAF**——headless 后台 rAF 被节流;勿依赖 RR `<Link>` 的 ref 转发)。check-data+build+128 测试+浏览器走查(门户派生/caveat/续读/TOC 双向滚动)全绿。**后续 P1 建「概念/书名→章节」全站映射表 → P2 跨组搜索/互见/我的聚合 → P3 体验传播 → P4 义理专题,见 review 文档 §5**
- [x] P1 共享底座:全站书目索引 + 延伸跨组书名自动链(已发 v1.41.0,零内容变化)——`src/features/reader/booksIndex.js` 枚举 10 组全部 **55 部书 + 易经总目**(corpus 站走 corpusTexts、道藏走 dao/texts.json、易经单条链 `/classics`)→ 规范 `{group,corpus,slug,title,alias,aliases,href,status…}`,导出 `ALL_BOOKS`/`bookByTitle`/`globalBookRefs`。**这是 review §1 的「概念/书名→章节」共享底座**,后续跨组搜索(B1)、我的聚合(B3)、义理专题/术语表均复用一表。**首个消费者=跨书互见自动链(B2 自动链半)**:两个 `YanyiBlock`(corpus + dao)改用 `globalBookRefs` 替原 per-group `buildRefs`(已删),延伸里的他派书名即**跨组成链**——韩非子延伸提「老子」点到 `/dao/daodejing`(经 `EXTRA_ALIASES` 别名)、提「孟子」点到 `/ru/mengzi`,激活存量 1900+ 处跨派学脉。书名全站唯一(无碰撞)故跨组安全;`linkifyBooks` earliest-then-longest 处理子串(「大学问」不被「大学」截断)。与 owner「放弃硬隔离」一致,不改导航分组语义、不触各组铁律。`booksIndex.test.js` 7 例(枚举/唯一/href/查名/排自链/括注剥离/linkify 跨组)。**B2 概念互见锚(精选 30–50 处)、B1 跨组搜索、B3 我的聚合待后续批次;新加书/别名改 EXTRA_ALIASES,全站书目自动纳入**
- [x] P2 跨组搜索(已发 v1.42.0,零内容变化)——review §1/B1。`corpusSearch.js` 加 `ensureAllIndexed`/`searchAllCorpora`:横扫 9 个 corpus 站(都在 corpus chunk)+ 道藏(**动态 `import('../dao/daoSearch.js')`,保 dao 数据层不被静态拉进 corpus chunk**,首开全站搜索时才载),逐站跑各自 `searchCorpus`/`searchDao`、每站取前 5 条、**按组分栏**(组标签取 portalTitle 去「研读/研习」:释典/儒典/法家…,道藏单列),跨组 item id 加 corpus 前缀保 key 唯一。`CorpusSearchPalette` 加 `本站 / 全站` 段控(`.search-scope`,active 着站点主色;默认本站,切换即重建索引,全站态空状态出跨派概念 chips 无为/格物/致良知/兼爱…)。**易经走自己的 SearchPalette(卦爻形态不同)暂不并入**。验证:浏览器 `/ru` 全站搜「无为」出 10 组(释典/儒典/心学/法家/墨家/兵家/纵横/中医/谋略/道藏),点道德经结果跨组落 `/dao/daodejing/2`;`/dao` 本站搜「无为」仅道藏诸经、原类型分组(正文/注疏/延伸)不泄漏。build+135 测试+check-data 全绿。**默认本站(隔离保留),全站为 opt-in,与 owner「放弃硬隔离」一致;新 corpus 站自动纳入 ALL_CORPORA 需手加键**
- [x] P2 总门户「我的研读」全站聚合(已发 v1.43.0,零内容变化)——review §1/B3。`booksIndex.js` 加 `bookBySlug`(slug 全站唯一)+ `chapterHref`(分章走 `/章号`、短经单页走 `#corpus-ch-章`)+ rec 补 `singlePage` 字段;新组件 `src/features/reader/PortalStudyTrail.jsx` 挂总门户 `/hexagram`(卡片下、关于上),跨组聚合 localStorage 足迹:**继续读**(`reading` slug→ch 经 booksIndex 解析回各组书,带站点徽标 + 各自 sectionUnit 卷/章/篇,链到该章)+ **★收藏/✎批注计数** + **最近收藏**(`corpusMarks` 按 `at` 降序前 5,短经单页锚正确)。补「同时读佛+儒+中医的人无处看全部足迹」缺口。无足迹整段不渲染(`return null`,新访客门户保持清爽);易经经传走自身 /me(slug 不在书目索引)不并入。验证:浏览器注入跨组 reading+marks → `/hexagram` 出继续读 3 组(孟子/儒典·道德经/道藏·韩非子/法家,href/单位/徽标皆对)+ 收藏 3·批注 1 + 最近收藏(心经 `#fo-ch-1` 单页锚正确、按时间降序);清空后整段消失。build+135 测试全绿。**新读经站/书自动纳入(booksIndex 派生);足迹键已在 storage 导出白名单**
- [x] P2 跨书互见·义理互见锚(已发 v1.44.0,零内容变化)——review §1/B2 后半(自动书名链已于 v1.41.0 落地,P2 至此收尾)。新建 `src/data/concepts.json`:**跨派核心概念聚类**,每聚类列各书代表章(loci),**所有 locus 均经原文 grep 验证含该概念**。当前 5 聚类:人性之辨(孟子告子上↔荀子性恶篇↔传习录卷中)、格物致知(大学经↔近思录致知↔大学问↔传习录卷上)、兼爱与仁爱(墨子兼爱上↔论语颜渊↔孟子滕文公下辟墨)、无为(道德经卅七↔庄子应帝王↔文子卷一)、仁与四端(论语里仁↔孟子公孙丑上↔近思录克己)。`seeAlso.js` 的 `seeAlsoFor(corpus,slug,ch)` 返回同聚类他书代表章(不自链,经 booksIndex `chapterHref` 解析含短经单页 `#corpus-ch-N` 锚);`SeeAlsoBlock.jsx` 渲染章末「义理互见 · 概念」块(概念释 + 跨组链接),挂两个 `YanyiBlock`(corpus+dao,延伸或互见任一有则渲染)。与「桥」同精神:**显式人工策展、只在经典互指处给跳转,不泛滥自动链**。`seeAlso.test.js` 4 例;浏览器验证正反双向(孟子↔荀子↔传习录)、组内(道德经→庄子/文子)、4 成员(大学问→其余 3,含大学单页锚)、空章不渲染。build+139 测试全绿。**新增聚类务必 grep 确认目标章确含概念、取最经典之篇;concepts.json 后续复用于义理专题(#150)、术语表(#151)**
- [x] P3 读经渲染:韵文断行 + paged content-visibility(已发 v1.45.0,零内容变化)——review §3。**#143 诗体经按句读换行**:texts.json 加 `verse:true`(黄庭内景经,七言韵语);`AnnotatedText` 加 `verse` 参——纯文本段按句读 `(?<=[，。；！？])` 切片、句末插 `<br/>`(去末尾多余)、**注释词跨行不受影响**(锚定/词典两模式皆兼容);`ClassicText` 加 `verse` prop(锚定走 AnnotatedText、无注纯文走 `plainVerse`),`ClassicReader` 透传至两处 ClassicText,`DaoReadPage` 传 `verse={!!meta.verse}`;CSS `.classic-text__original--verse{line-height:2.2}`。**#144 分章长章逐段离屏跳过**:CSS `.read-page:not(.dao-single) .read-para{content-visibility:auto;contain-intrinsic-size:auto 160px}`(坛经机缘品 175 段、荀子大略 109 段免字号/译文切换掉帧;锚点/搜索仍可达,单页已由 `.dao-single__chapter` 处理)。验证:黄庭章1 出 12 `<br/>`(13 句各行)、4 注疏仍在;道德经(非 verse)无 verse 类;两书 paged 段 `content-visibility:auto`;build+139 测试全绿。**诗体新书设 `verse:true` 即生效(dao 已接;corpus 走 CorpusReadPage 时同样一行 `verse={!!meta.verse}` 待接)**
- [x] P3 单页长经 scroll-spy + 移动端 select 回显(已发 v1.46.0,零内容变化)——review §3。`ClassicReader` 单页模式(金刚经 32 分/黄庭 36 章铺一页)加滚动联动:`activeAnchor` 由滚动处理器算「顶部越过工具条线 130px 的最后一章」(rAF 节流、挂载即同步算一次故不依赖后台 rAF;比 IntersectionObserver 带状 rootMargin 更稳——顶部/章间空隙不留陈值),点亮侧栏当前章(`.read-toc__item--active`,复用 #138 自动滚入视野,effect deps 加 activeAnchor)+ **回显移动端 `<select>`**(原 `value` 单页恒空 → 改 `activeAnchor`,占位符仅 activeAnchor 空时出);section 加 `data-no`。验证:金刚经顶部=法会因由分第一、50%=离相寂灭分第十四、滚回顶部复位第一;build+139 测试全绿。**新单页长经自动获 scroll-spy(走 ClassicReader single 模式即可)**
- [x] P3 书架篇幅档 + 底本凡例(已发 v1.47.0,零内容变化)——review §2/§3/§4。**#148 篇幅档信号**:`scripts/gen-book-sizes.mjs` 离线统计各 classics 原文字数 → `src/data/bookSizes.json`(55 部,心经 348→韩非子 12.8 万);`bookSizes.js` 的 `sizeTier(slug)` 按字数粗分 短<4k/中<20k/长(解决 sectionUnit 口径不一〔1 卷 vs 81 难 vs 555 段〕无法横比),`ScriptureShelf`+`DaoHomePage` 卡片 meta 出 `.book-tier` 弱色 pill(短绿/长赭);**易经门户差异化**:registry portalDesc 加「筮例(全站唯一含推演的组)」。**#146 底本与凡例**:`AboutPage` 加「底本与凡例」节(底本取通行本原则 + 道德经王弼本/心经玄奘略本/坛经宗宝本/大学中庸礼记古本/系辞九章/参同契卅五章/列子文子黄庭阴符成书存疑 等择本要点,详见各书撰人小传),并修正过期搜索说明(「只搜本组」→「可切本站/全站」)。验证:`/ru` 论语孟子荀子=长篇·大学孝经=短篇·中庸=中篇;`/about` 出底本与凡例节。**加书后重跑 `node scripts/gen-book-sizes.mjs` 纳入篇幅档**
- [x] P3 留存:铁律横幅 + 备份提示 + 里程碑(已发 v1.48.0,零内容变化)——review §2/§3。**铁律/真伪声明横幅**:`ScriptureShelf` 加 `disclaimer` prop → 独立 `.shelf-disclaimer` 横幅(左竖线着站点主色,权重够不被略过);中医/谋略首页把 ⚠ 段从 `subtitle` 拆出改传 `disclaimer`。**备份提示**:`storage.exportData()` 记 `lastExportAt`(不入 DATA_KEYS),新 `getStudyStats()` 返回 收藏/批注总数 + 是否曾导出;`CorpusMePage` 当总数 ≥8 且从未导出时出非阻断 `.me-backup`「建议导出备份」,导出后即消。**里程碑**:`milestoneText(total)` 按收藏批注总数给鼓励文案(≥10 日拱一卒 / ≥20 渐有所积 / ≥50 积学已富 / ≥100 蔚然成观),挂 `/me` 标题下。验证:`/zhongyi`+`/moulue` 出横幅;注入 10 收藏 → `/zhongyi/me` 出里程碑「已积 10 处…」+备份提示,set lastExportAt 后提示消失。build+139 测试全绿。**新读经站 disclaimer 走 ScriptureShelf prop;备份/里程碑各 /me 自动生效(走 getStudyStats)**
- [x] P4 义理专题 · 跨派概念页(已发 v1.49.0,零内容变化)——review §4(#150 专题 + #151 术语表,二者「同源 concepts.json」一页两得)。新页 `src/features/ConceptsPage.jsx`(路由 `/concepts`),复用 v1.44.0 的 `concepts.json` 把 5 个概念聚类铺成专题卡:概念名 + 义理释(gloss)+ 各书代表章链接(经 booksIndex `chapterHref` 解析,跨组对读)——既是义理专题(每卡一题)又是名相术语表(概念→各书章)。挂总门户(`master-portal__links`「义理专题 · 跨派概念」)+ 走**中立外壳**(App `NEUTRAL_PATHS={/hexagram,/concepts}` 驱动 `isPortal`,不套分站 nav/搜索/底栏,域名着陆豁免)。验证:`/concepts` data-site=portal·无分站 nav·5 卡(人性之辨→孟子告子上/荀子性恶/传习录卷中…仁与四端→论语里仁/孟子公孙丑上/近思录克己),门户链接 `/concepts`;build+139 测试全绿。**扩 concepts.json 聚类即自动增专题/术语条目(义理互见、术语表、专题三处同步)**
- [x] P3 分享传播:OG 卡 meta + 段落金句卡(已发 v1.50.0,零内容变化)——review §2。**OG/分享卡 meta**:`index.html` 加站点级静态 `og:*`/`twitter:card=summary`/`description`(图用 `pwa-512.png`)——分享任一页链接即出书名摘要卡(原为裸链接);逐页动态需构建期预渲染,后续再议。**段落金句卡**:新组件 `src/features/reader/QuoteCard.jsx`——把一段 原文+译文+《书名·章》渲成 SVG 卡(观象朱印、衬线居中、句读折行),`下载图片`走 SVG→canvas→PNG(await `fonts.ready` 保中文衬线,失败可截图兜底);`ClassicReader` para-actions 加 🖼 动作(markCtx 段落),`cardSource(no)` 署名,`bookTitle` prop 由 `DaoReadPage`/`CorpusReadPage` 传(顺带补全 corpus 的 `verse` 透传)。验证:`/ru/lunyu/1` 点 🖼 出卡(印「观象」、原文 3 行、译文、署「《论语》· 学而第一」、下载按钮),OG meta 入 dist。build+139 测试全绿。**新读经站走 ClassicReader 即自动获金句卡;OG 为站点级,逐页动态待预渲染方案**
- [x] **产品/UX/国学 review(docs/product-ux-review.md)全 18 项收官(v1.40.0→v1.50.0,均零内容变化)**:P0 修债 5 项 → P1 共享底座(全站书目索引)→ P2 跨组增益(跨组搜索/跨书互见/我的聚合)→ P3 体验传播(scroll-spy/韵文/content-visibility/凡例/篇幅档/留存/金句卡 OG)→ P4 义理纵深(专题术语页)。**核心数据底座**:`booksIndex.js`(全站书名→链接)+ `concepts.json`(跨派概念→各书章,grep 验证)一表多喂(自动链/搜索/我的/互见/专题/术语)。后续若再扩书:重跑 `gen-book-sizes.mjs`;若加诗体书设 `verse:true`;若加概念聚类改 `concepts.json`(义理互见+术语+专题三处自动同步)
- [x] 二十一期·《赛博:百家争鸣》批1 基建 + 人性之辨切片(已发 v1.51.0)——跨派对辩模块(design-v21)。**离线、不评输赢、中医/谋略不入场、每句锚真原文**。数据层 `src/data/debates/`(`index.json` 列已建辩 + 46 题路线图;一辩一文件,`renxing.json` 人性之辨 9 轮:孟子性善↔荀子性恶↔阳明良知,每 turn 的 `cite.quote` 均 grep 验证为该章原文精确子串)。`debates.js` 懒加载 + `groupAccent`(组→根级 accent token,portal 外壳下直接用)+ `pickDailyDebate`(日期 hash,批2 用)。`DebatePage`:**圆桌+论辩图谱合一 SVG**(印章环坐、`rebut` 成朱线、active 节点脉冲、开坛错峰淡入、边 stroke-dashoffset draw-on)+ **回合流**(立论/诘难/申辩分段,逐句展开:▶自动/下一句/跳收束,每发言挂论点+据原文+读原文链)+ **收束** coda+题词。`DebateListPage` 辩题卡 + 46 题路线图。中立外壳(App `isNeutralPath` 加 `/debates`、`/debates/:id`)+ 门户链接。`check-data` 加 `checkDebates`:**cite 子串校验、禁中医/谋略入场、rebut 合法、轮数 ≤100、论点无胜负词、index 一致**(过:1 辩 9 轮 3 家次)。**坑**:SVG `fill="var(--x)"` 作为presentation 属性不认 var(),须用 `style={{fill:...}}`(CSS 属性)。验证:浏览器 `/debates`(1 卡+10 类+45 路线)、`/debates/renxing`(3 印章环坐、跳收束出 9 轮+3 朱线+coda、据链 `/ru/mengzi/11` 等)、build+139 测试全绿。**批2:每日一辩弹窗 `DailyDebate`+三处入口;批3+:46 辩内容并发 workflow 铺开(每家一 agent 据真章节起论+校 agent 核子串铁律+编者 agent),`gen-debate-wf` 待建**
- [x] 二十一期·批2 每日一辩弹窗 + 入口(已发 v1.52.0)——`DailyDebate.jsx`:首页(/)+ 总门户当日首访自动弹一次(日期 hash 定题、弹出即记 `dailyDebateSeen` 当日不再弹),纸色卡 + 印章 + 进去看/今日不再。**经 `createPortal` 渲染到 `document.body`**——逃离页面内带 transform/content-visibility 的祖先,`position:fixed` 方相对视口(否则 overlay 被困、card 被压窄)。入口:门户「赛博·百家争鸣」链(批1)+ `concepts.json` 概念卡「→ 入争鸣」(ConceptsPage 按 `topic.concept===cluster.term` 匹配,故 index topic 须带 `concept` 字段)。storage 加 `getDailyDebateSeen/markDailyDebateSeen`(不入 DATA_KEYS)。验证:清 flag → `/` 自动弹人性之辨(3 印章、tag 日期)、标记后不重弹、概念页出 `/debates/renxing` 链;1280 视口 card 376px 居中。**坑**:preview iframe 视口偶塌缩成 48px 宽致 fixed/vw 失准,resize 即恢复(非代码问题)。build+139 测试+check-data 全绿。**批3+:46 辩内容铺开,`gen-debate-wf`(每家一 agent 据真章节起论+校 agent 核 cite 子串与铁律+编者 agent)待建;阅读器章末入口待加(需 index 带 cite loci 或载文件)**
- [x] 二十一期·批3 概念五辩(已发 v1.53.0)——**手工策展** `concepts.json` 5 聚类对应的 4 新辩(连同批1 人性之辨,5 聚类全有辩):兼爱与仁爱(墨子兼爱上↔论语颜渊↔孟子滕文公下,6 轮)、格物致知(大学经↔近思录致知↔大学问↔传习录卷上,6 轮)、无为(道德经卅七↔庄子应帝王↔文子卷一,5 轮,道家内部相发)、仁与四端(论语里仁↔孟子公孙丑上↔近思录克己,5 轮,儒门相承)。**每 turn 引文先 grep 坐实为该章原文精确子串再写**(共 31 轮全 done)。index.json topics 补 4 条(带 `concept` 字段供概念页互链)+ planned 删 3 条已建(爱有差等否/格物之解/道家三调,余 42)。验证:`/debates` 5 卡、`/debates/jianai` 3 印章(观兼+观仁×2)6 轮 2 朱线 coda、`/concepts` 5 张概念卡「入争鸣」链全亮;check-data「5 辩 31 轮 15 家次」(cite 子串/铁律/无胜负词全过)+ build+139 测试全绿。**续辩沿此:grep 坐实引文→写 `<id>.json`→index 补 topic(带 concept)→check-data;或建 `gen-debate-wf` 批量**
- [x] 全站功能/交互/逻辑优化(已发 v1.25.0,零内容变化)——四片并发审查汇总 `docs/ux-review.md`,owner 拍板「保留全局 logo、放弃硬隔离」后按批 A–E 全实施:**批A 移动端读经急救**(`.read-toc` @≤768px 隐藏后补 ClassicReader「章节下拉」;消息卦/河图洛书/方圆图脱离 hover-only,补 onClick/onFocus/tabIndex/键盘;长单页经 `content-visibility:auto`;越界章号带返回链接);**批B PWA 部署稳健**(`vite.config` globPatterns 收窄,precache 7.7MB→330KB、数据 chunk 改 runtime SWR 恢复网络层隔离;`src/features/ErrorBoundary.jsx` + main.jsx `vite:preloadError→reload` 根治发版白屏);**批C 路由/工作台**(`siteForPath` 边界判断〔=prefix 或 prefix+'/'〕,404 不套别组皮;singlePage 章路由重定向;工作台 `?dong=` 去重取整、`?method` 持久化、数字键无卦不响应);**门户中性外壳**(`/hexagram` data-site=portal,不渲染分站 nav/搜索/底栏);**批D 搜索/一致性**(SearchPalette/CorpusSearchPalette 锁滚动+还焦+aria-modal+useDeferredValue+命中定位;**道藏接全站搜索** `daoSearch.js`+`DaoSearchPalette`;`/` 键 guard 补 IME/select/可编辑区;storage 白名单校验+跨标签同步;singlePage 不写续读;篇目骨架);**批E 打磨**(搜索挂载由 registry `searchKind` 单一来源派生;移动端单卦详情底栏 `match` 高亮;互动 SVG `max-width:100%`;撤销删除按 id 排序;走查环 `pointer-events:none`;伪书徽标暗色加粗)。**新增页面/重交互组件:互动信息勿仅挂 hover(补 click/键盘);新读经站搜索由 registry `searchKind` 驱动;长页优先 content-visibility;新增懒加载页天然受 ErrorBoundary 兜底**
- [x] 全站易用性/功能性优化三层(已发 v1.26.0→v1.29.0,零内容变化)——第三轮四片并发审查汇总 `docs/ux-opportunities.md`,owner 拍板「全部按 Tier 推进」后逐层发版:**Tier 0 快赢(v1.26.0)**:门户卡片渲染既有 `portalDesc`;**全站设置浮层** `src/features/SettingsSheet.jsx`(nav 齿轮任何站就地开:主题/字号/译文/数据导出导入+隐私说明+导入 confirm,读经站去 ☯ 不再甩进易经);学堂总进度+「从这里开始/继续学习」;今日卦「以此卦推演」+工作台「复制本卦链接」;长章段号默认(论语外 >3 段)+篇目页「继续读」+读至章高亮;搜索空状态经名 chips;logo 门户 hover 提示。**Tier 1 中等(v1.27.0~v1.28.0)**:读经 ←→ 键翻章;正文行宽设置(`--read-w`);学堂 8 篇 `LearnNextLink`「下一篇」接力;QuizCard「错题重练」(子集不计「通过」)+鼓励文案;原文/译文左右对照(`transLayout`,ClassicText `--side` 网格);注疏「本章一览」折叠(ChapterNotes);我的·研习仪表盘。**Tier 2 结构性(v1.29.0)**:**读经站「记」体系**——ClassicReader `markCtx` 驱动每段 ★收藏/✎批注(锚 `corpus:slug:章:段`,storage `corpusMarks`/`corpusNotes` 纳入导出导入)+ 每读经站「我的」`CorpusMePage`(`/<key>/me`:续读+收藏+批注回链)+ nav ☯ 回归读经站。9 读经站+道藏全覆盖。**新增设置项加 `DEFAULT_SETTINGS`+白名单;读经新交互走 markCtx 锚;新读经站自动获 /me(App 路由表数组加 key)**
- [x] 十五期:三教分站与分组隔离(docs/design-v15.md,已发 v1.16.0→v1.16.1)——架构段(佛/儒经文内容另起内容期):registry 每站加 `group`,门户/切换按钮/移动切换只列当前组(`sitesInGroup`),**佛(fo,观空,缁黄)/儒(ru,观仁,苍绿)各为独立单站组,与易道·彼此两两零可见链接**;`activeGroup` 域名优先(`HOST_GROUPS`:`tao.gavingao.cn`→易道·`con.gavingao.cn`→儒·`bud.gavingao.cn`→佛)路径兜底,命中则着陆重定向本组首页(一份构建集中部署、三域名各锁一组);`src/features/reader/ScriptureShelf.jsx` 通用书架,佛/儒 texts.json 骨架(佛:心经/金刚经/坛经;儒:四书)status pending。**隐藏门户**(owner 自用、无任何站内链接指向):秘密路径 `MASTER_PORTAL_PATH='/hexagram'` → `MasterPortalPage` 列三组卡片,`groupEntryHref` 生产域名出绝对跨域 URL、dev 出相对路径,着陆重定向对该路径豁免。**经文内容接入照 v9 §4,佛站守研习不宣化、不下吉凶断语;加新站照头部「如何加一个新站」并按 group 隔离**
- [x] 十四期:分站平台化(docs/design-v14.md,已发 v1.15.0)——零内容零功能语义变化的重构:站点注册迁至 **src/sites/registry.js**(SITES manifest,App.jsx 读它驱动品牌/导航/门户/移动栏/搜索);主题从 .app-shell--dao 特例改 **[data-site] 驱动**(app-shell 挂 data-site={key},新站加 `[data-site=x]` 主色块);三阅读器(经传/道藏逐章/短经单页)抽成**通用 src/features/reader/ClassicReader.jsx**(paged/single 两模式),各页改薄包装传 loader/anchors/yanyi/header;check-data 书单从 texts.json 派生(加书免改校验)。**加新站照 CLAUDE.md 头部「如何加一个新站」六步,平台代码零改动**
- [x] 十三期:道藏单页阅读与每章延伸(docs/design-v13.md,已发 v1.13.0→v1.14.0)——短经(清静经/感应篇/阴符经,texts.json singlePage 标记)走 DaoSinglePage 单页阅读器(题解+全文一页铺开+左章节锚点+字号译文工具条),长经保留逐章 DaoReadPage;**每章延伸 src/data/dao/yanyi.json**(人工脱锚 registry,slug→章号→段落,照 v9 §4 分级,只讲思想/故事/源流不作信仰宣化吉凶断语),YanyiBlock 组件(azure 玄青)挂两处阅读器章末,六部 128 章全覆盖(道德经81+庄子7+参同契35+清静经1+感应篇1+阴符经3);check-data 校验+覆盖仪表。**新增/改延伸照 v9 §4,改完必过 check-data**
- [x] 推演上手引导(docs/design-v12.md,已发 v1.12.1→v1.13.0)——降低工作台门槛,零数据新增:**操作指导**=常驻步骤条(①起卦→②标动爻→③读断法,按 hex/动爻状态点亮)+「示范一卦」走查(载入乾·九五动,GuideTour 自写组件逐区描边讲解,瞬时滚动+立即量+rAF校准定位);**知识讲解**=学堂「推演入门」专篇(/basics/tuiyan,起卦/四词/六法两类/变占规则表引《易学启蒙》/三层解卦)+工作台引导框 TermTip 内联概念(本卦/动爻)。**新增起卦法/断法须同步更新推演入门篇与步骤条语义**
- [x] 解卦三层(已发 v1.11.0→v1.12.0)——工作台断卦卡(RuleCard)的每句所占经文叠三层补充,**只释卦象不断吉凶(守 §9 禁算命口吻)**,数据全来自既有 hexagrams.json + zhushi(-anchored),无新增内容:①经文+译文 ②传文 commentary:卦辞配彖(「彖传释卦」)、爻辞配小象(「小象释爻」)、用九用六配各自小象,参考折叠卦辞展开亦带彖 ③字词注疏:卦辞/爻辞挂词典模式(annotate→zhushi.json)、彖/大象/小象挂锚定模式(anchorRef/commentary.hexId→getHexAnchors),悬停出注。engine/divination.js 保持纯净只挂元数据(commentary/annotate/anchorRef),锚点查询在 RuleCard;配 divination.test.js 9 例覆盖 n=0..6 全断法
- [x] 十一期:工程打磨期(docs/design-v11.md,已发 v1.10.0)——零内容零功能语义变化:正文字体自托管(@fontsource/noto-serif-sc,woff2 不进 precache 走 runtime CacheFirst);路由全懒加载+搜索面板按需加载(入口 JS 780→245KB);usePageTitle 按页标题(易经缀「观象」道藏缀「观道」,23 页接入);apple-touch-icon;暗色走查零缺陷。**新增页面必须接 usePageTitle;新增重数据组件优先懒加载**
- [x] 十期:工具与交互期(docs/design-v10.md,已发 v1.9.0)——纯代码无新内容:全局搜索八组多源索引(searchIndex.js,经传 chunk 懒加载异步补建,道藏不入索引);验占闭环(占例 outcome 字段+我的页回填统计);筮例重演(卦例图「在工作台重演」走 ?gua=&dong=);卦画闪卡 /basics/guahua(识卦/画卦,干扰项取错综与一爻差);先天方圆图 engine/xiantian.js+FangyuanView(总览 ?view=fangyuan,**圆图方图布列照 v10 §5 规则表,配单测**);续读入口+卦页 ←→/横滑翻页;PWA(vite-plugin-pwa,precache 全站离线)。LEARN_TOPICS 注册表在 src/features/yijing/learnTopics.js(学堂/我的/搜索共用)
- [x] 九期:筮例与故事(docs/design-v9.md,已发 v1.8.0)——春秋筮例 21 条(左传 18+国语 3,筮占 15·引易 6):原文走摘录式管线 scripts/fetch-shili.mjs(page+起止 mark 切片,标点各公不一,**严禁手改原文**),背景/全译/解读为人工内容(scripts/authored/shili.json),页面 /shili 列表+详情(卦例图 movingLines=binary 异或)+卦页关联回链;爻辞商周史事 8 节(src/data/yijing/shishi.json 人工 registry+/basics/shishi+卦页「史事:」回链);人物志易学十家(renwu.json+源流页时间线扩明代节点+同页小传锚点+站内互指 15 处,**链接禁入 /dao**);道藏 texts.json 六部 authorNote 撰人小传(DaoTextPage 题解展示,纯文本)。**筮例/史事/人物的脱锚叙述照 v9 §4 四级分级(共识直写/考证标出处/存疑明示/宁缺毋滥);check-data 校验筮例≥19·史事卦爻位·人物≥10 家**

## 常用命令

```bash
npm run dev          # 开发服务器
npm run build        # 生产构建
npm test             # vitest(M2 起引擎必须有单测)
npm run data:fetch   # 从维基文库抓取原文并重新生成数据(带本地缓存,可随时重跑)
npm run check-data   # 数据校验,任何数据变更后必须通过
```

## 数据规则(硬约束)

1. **经文原文一律来自数据管线,严禁手改、严禁凭记忆补写。** `src/data/yijing/hexagrams.json` 和 `classics/*.json` 是 `npm run data:fetch` 的生成物(来源:维基文库《周易》,繁转简,通行本)。发现原文问题改 scripts/fetch-data.mjs 的解析逻辑后重跑,不要直接编辑生成文件。
2. 人工内容(拼音、提要、译文)写在 `scripts/authored/*.json`,由 data:fetch 合并进生成物。改完必须重跑 `data:fetch` + `check-data`。
3. `scripts/lib/hexagram-table.mjs` 是 64 卦基准表(卦序/卦名/上下卦/binary),它与抓取内容互相校验。binary 一律**自下而上**(下标 0 = 初爻),这是全项目约定,任何组件和引擎都不得违反。
4. 全角标点注意:工具链可能把输出里的全角逗号/冒号静默转成半角。代码里匹配全角标点必须用 `：`(:)、`，`(,)、`；`(;)转义,不要写字面量;中文数据文件写完后检查标点。

## 译文工序(已完成,修订时仍照此)

64 卦译文已全部完成。日后修订单卦译文时:

1. 打开 `src/data/yijing/hexagrams.json` 找到该卦,**对照每个 original 字段直译**——译文必须从眼前的原文译出,不是从记忆里背。
2. 改 `scripts/authored/translations.json`(judgment/tuan/daxiang/lines×6;乾坤另有 use 字段为用九/用六)。
3. 风格按设计稿 §9:平实直译、一段对一段、禁算命口吻、歧义取程朱主流注解。
4. 重跑 `npm run data:fetch && npm run check-data`。

经文字词注释在 `src/data/yijing/zhushi.json`(人工维护,长词优先收录),由 AnnotatedText 组件挂在卦辞与爻辞原文上。

## 代码组织

- 页面与组件:`src/features/yijing/`(组件清单见设计稿 §4)
- 推演引擎:`src/features/yijing/engine/`,纯函数,规则见设计稿 §6,必须配单测(含 §10-M2 列出的自检用例)
- 数据:`src/data/yijing/`(只读生成物 + trigrams.json)
- 不引入 UI 组件库;样式用原生 CSS + tokens(已在 src/index.css)
- localStorage 键前缀 `guanxiang.v1.`,读写过 storage 薄封装(设计稿 §7.3)

## 部署

默认按根路径托管(Vercel/Netlify/Cloudflare Pages 直接可用)。如改用 GitHub Pages 子路径,需同时设 vite.config.js 的 `base` 和 Router 的 `basename`。

## 已知差异与待办

- 系辞下底本为九章分法(孔颖达),与设计稿写的十二章不同,以数据为准,check-data 已按 9 章校验。
- 序卦分上/下两篇;杂卦一篇(抓取时已剔除维基文库的「校诂版」对照章节)。
- 乾坤的文言传是扁平段落数组,未按爻分节,详情页直接顺序展示即可。
- hexagrams.json 的 palace 字段**不再人工填**:八宫归属由 engine/bagong.js 运行时计算(v2 §5.1),数据文件保持 null 不动。
- 周易参同契底本为维基文库 35 章分法(非传统上中下三篇),texts.json sections 与 check-data 均按 35 章;阴符经正文抓自 Page: 校对页(主页面是 djvu 转嵌)。
- 道德经底本为「道德經 (王弼本)」:经文按王弼注本分段(注文在管线清洗时剔除),故每章段数多于通行排印本,译文与注疏按此分段对位。
